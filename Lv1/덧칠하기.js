const n = 8;
const m = 4;
const section = [2, 3, 6];

function solution(n, m, section) {
  let count = 0;

  let totalArray = Array(true).fill(n);

  return totalArray;
}

console.log(solution(n, m, section));
